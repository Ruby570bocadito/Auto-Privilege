# Informe de ronda — AGENTE DIRECTOR

- **Fecha y hora de la ronda:** 2026-09-15 00h03 (Europe/Madrid)
- **Rol:** 1 de 4 (Agente Único — Director)
- **Repositorio:** Ruby570bocadito/Auto-Privilege, rama `main`
- **Commit base auditado:** `531bdc9` — "ci: add gofmt check step (parity with Vesper quality gate)"

## Informes leídos antes de empezar

Primera ronda documentada: no existía ningún informe previo en `docs/agentes/` (carpetas creadas en esta ronda). Como sustituto del contexto histórico se han revisado los 5 commits de `main`, el contenido íntegro de los 11 ficheros `.go`, los tests, CI, Dockerfiles, el lab rootless y ambos README. No hay contexto previo que arrastrar; esta ronda establece la línea base.

## Resumen ejecutivo

El proyecto está funcionalmente sano: compila con Go 1.26, `go vet` y `gofmt` limpios, 23 tests en verde (verificados en esta ronda, no asumidos), CI presente y una arquitectura clara en 3 fases (scan → enumerate → exploit). Sin embargo, la auditoría ejecutando el binario real ha destapado **dos promesas documentadas que el código no cumple** (el campo `summary` del modo `--json` que el README usa con `jq`, y los binarios de release que se anuncian pero nadie construye), un **bug de salida duplicada** de hallazgos, y una rama remota obsoleta (`audit-improvements`) que induce a confusión. Ninguno de estos hallazgo es crítico de seguridad; todos dañan la credibilidad del principio rector del proyecto ("honest results").

## Estado general del proyecto: 🟡 AMARILLO

Justificación: el núcleo es sólido y está bien probado (verde), pero la discrepancia entre lo que la documentación promete y lo que el binario hace reales es exactamente el tipo de deuda que este proyecto no se puede permitir, porque su propuesta de valor es la honestidad de sus resultados. Se corrige en esta misma ronda.

## Verificación empírica realizada (no solo lectura de código)

| Comprobación | Resultado |
|---|---|
| `go build` con Go 1.26.0 | OK |
| `go vet ./...` | OK, sin avisos |
| `gofmt -l .` | Sin ficheros pendientes |
| `go test -count=1 ./...` | OK — 23/23 |
| Ejecución real read-only en este contenedor | 11 hallazgos, 2 explotables, exit 0 |
| `--json` → claves del objeto | NO existe `summary` (README lo promete) |
| `--dry-run` sin `--exploit` | NO imprime el plan (README lo promete) |
| Salida terminal | Hallazgos impresos **dos veces** (durante scan y en "── Findings ──") |

## Revisión por agente (trabajo heredado en el repo)

- **Implementaciones:** el overhaul v1.1.0 (`1101cce`) dejó la CLI moderna, el lab rootless y los vectores honestos. Deuda de producto detectada: los hallazgos `CRED` que hoy se marcan `exploitable=true` (configs con contraseñas, history con secretos, metadata cloud) **no generan ningún vector** en la fase 2 — el operador ve un hallazgo que luego desaparece del plan y del informe de vectores.
- **Pulimiento:** buen estado general (cero ficheros muertos, gofmt limpio, `.gitignore` correcto, dependencias externas cero y sincronizadas). Puntos sucios: salida duplicada de hallazgos; el comentario de `ui.go` referencia un `scripts/gen_ap_banner.py` que no existe en el repo; los badge de tests de ambos README están hardcodeados a "23".
- **Bugs/Seguridad:** la ronda v1.1.0 corrigió 15+ bugs reales y los tests de regresión lo demuestran. Quedan 5 defectos funcionales verificados en esta ronda (detalle en el plan).

## Riesgos detectados

| # | Riesgo | Prioridad |
|---|---|---|
| R1 | README(es/en) documenta `jq '.summary'` sobre la salida `--json` pero `jsonReport` no tiene ese campo → el ejemplo del README devuelve `null` | ALTA |
| R2 | README promete binarios de release (linux amd64/arm64) pero no existe ningún workflow de release → el enlace `releases/latest/download/...` es un 404 para el usuario final | ALTA |
| R3 | `--dry-run` solo no muestra el plan de ejecución (hay que pasar además `--exploit`), contradiciendo la ayuda y ambos README | MEDIA |
| R4 | Hallazgos impresos dos veces en modo terminal (ruido, aspecto de bug ante el usuario) | MEDIA |
| R5 | `scanSUID` marca como explotables binarios SUID cuyo dueño no es uid 0 (falso positivo directo al principio "honest results") | MEDIA |
| R6 | Hallazgos CRED sin vector asociado (el plan dry-run y el informe los omiten) | MEDIA |
| R7 | `AutoPrivilege.Print()` imprime en modo `--quiet` si alguien lo invoca (inconsistencia con el contrato "quiet = sin salida") | BAJA |
| R8 | El comando mostrado por el vector cron tiene el quoting roto (payload con comillas simples dentro de comillas simples) — un operador que lo copie a mano ejecutará basura | BAJA |
| R9 | Rama remota `origin/audit-improvements` obsoleta: basada en v1.0, su commit `afa786b` fue superado por el overhaul v1.1.0. Confunde a cualquier colaborador | BAJA |
| R10 | `isWritableByCurrentUser` ignora el caso euid 0 (root puede escribir todo) | BAJA |

## Decisiones tomadas en este rol

- No se ha tocado código en este rol (no había nada que bloqueara a los siguientes roles).
- Se decide NO fusionar `origin/audit-improvements`: su contenido está superado; se recomienda su borrado al mantenedor (queda fuera del alcance de esta ronda tocar ramas remotas ajenas a `main`).
- Versión: las mejoras de esta ronda justifican subir `Version` de 1.1.0 a 1.2.0 (se ejecuta en el Rol 2).

## Plan para los roles 2, 3 y 4 de esta ronda

- **Rol 2 — Implementaciones (3 mejoras):** (a) añadir `summary` al informe JSON tal y como el README lo documenta, con test; (b) workflow de release real (`release.yml`) que construya binarios estáticos amd64/arm64 al empujar un tag `v*`, cumpliendo la promesa del README; (c) vectores para hallazgos CRED (ssh-key ya existía; añadir configs, history y metadata cloud como vectores manuales), con test. Bump de versión 1.1.0 → 1.2.0.
- **Rol 3 — Pulimiento:** eliminar la impresión duplicada de hallazgos (consolidar en la sección "── Findings ──"); corregir el comentario huérfano de `ui.go`; actualizar badges de tests y ejemplos en ambos README; verificación final de gofmt/vet/tests y de `.gitignore`/dependencias.
- **Rol 4 — Bugs/Seguridad:** corregir R3 (`--dry-run` muestra el plan sin necesidad de `--exploit`), R5 (SUID solo explotable si el dueño es uid 0, con test), R7 (`Print` respeta `--quiet`, con test), R8 (comando cron con heredoc citado, con test), R10 (cortocircuito euid 0). Revisión de seguridad del pipeline de datos GTFOBins persistido.
