# Informe de ronda — AGENTE DIRECTOR

- **Fecha y hora de la ronda:** 2026-09-15 14h23 (Europe/Madrid)
- **Rol:** 1 de 4 (Agente Único — Director)
- **Repositorio:** Ruby570bocadito/Auto-Privilege, rama `main`
- **Commit base auditado:** `97d1b53` — "ronda 2026-09-15: revisión + mejoras + pulido + fixes" (ronda 8)
- **Directiva del mantenedor para esta ronda:** rondas MÁS LARGAS e INTENSAS, más rondas, y máxima carga en el Rol 2 (añadir y mejorar funciones reales y aplicarlas). Esta ronda es la más cargada hasta la fecha: cinco bloques de mejora completos.

## Informes leídos antes de empezar

Los cuatro informes de la ronda 8 (`docs/agentes/*/ronda_2026-09-15_13h02.md`). Heredan v1.6.0: `--baseline` (diff de endurecimiento), `--fail-on` (gate CI, exit 3), scanners PRELOAD/SUDOERS, 82/82 tests, READMEs es/en sincronizados. Backlog heredado: GIF re-grabación, tag del mantenedor, docker/ con Docker disponible, CVE-2022-2588 diferido, drop-ins individuales de sudoers.d, seeds de lab para los scanners nuevos.

## Resumen ejecutivo

Verificación empírica completa en `97d1b53`: build OK, `go vet` sin avisos, `gofmt -l` vacío, suite **82/82 PASS**, árbol limpio. Los informes de la ronda 8 coinciden con la realidad del repo. La auditoría de producto bajo la directiva de maxificar Implementaciones detectó que la herramienta había cerrado el ciclo medir→endurecer→verificar pero seguía sin poder **cuantificar** la mejora (no había métrica de postura), sin poder **subir a los dashboards** donde vivan los findings (no había SARIF), sin **acelerar** el escaneo (todo secuencial aunque getcap domina el coste) y sin cubrir tres familias de superficie de sesión (grupo escribible, hooks de login, exports NFS sin restricción de host, wildcards de cron). Además, el Rol 4 encontró un **bug real de CI**: el filtro de ramas de `ci.yml` estaba corrupto (`branches: ain]`) — la CI llevaba rondas sin ejecutarse en pushes a main ni en PRs. Se repara esta misma ronda.

## Estado general del proyecto: 🟢 VERDE

Justificación: suite íntegra en verde (109/109 al cierre), deuda documental cero, contratos de salida protegidos por tests y CI, detector de carreras limpio. Los defectos detectados (filtro CI) se reparan en la ronda.

## Verificación empírica realizada

| Comprobación | Resultado |
|---|---|
| `go build` + `go vet` + `gofmt -l .` | OK / sin avisos / vacío |
| `go test -count=1 -v` (base) | **82/82 PASS** |
| `go test -race -count=1` (cierre) | PASS — sin carreras |
| Árbol de trabajo al llegar | Limpio, HEAD `97d1b53` |
| Informes ronda 8 vs repo real | Coinciden |
| `.github/workflows/ci.yml` (inspección cruda) | **Filtro de ramas corrupto**: `branches: ain]` en push y pull_request — la CI no matcheaba `main` ni PRs |

## Riesgos detectados

| # | Riesgo | Prioridad |
|---|---|---|
| R44 | Sin métrica de postura: el diff dice CUÁNTO cambió (hallazgos nuevos/resueltos) pero no CUÁNTO IMPORTA — un HIGH explotable nuevo y un LOW informativo nuevo pesan lo mismo en el recuento. Imposible decir "la postura mejoró de 60 a 85" sin contar a mano | **ALTA** (directiva de ronda) |
| R45 | Sin integración con dashboards de code-scanning: el JSON propio es consumible con jq, pero GitHub Security tab / GitLab / visores SARIF no lo ingieren; los findings de la herramienta viven fuera del flujo donde los equipos ya miran | **ALTA** (directiva de ronda) |
| R46 | Latencia de escaneo secuencial: `getcap -r` (4x timeout) domina el coste en hosts con árboles grandes; en CI cada minuto es dinero y ronda tras ronda el scan corre en serie pese a que los scanners son independientes entre sí | **ALTA** (directiva de ronda) |
| R47 | Superficie de sesión sin cubrir: (a) `/etc/group` escribible → auto-incorporación a sudo/wheel/docker en el próximo login; (b) hooks de login escribibles (`/etc/environment` con LD_PRELOAD process-wide, `/etc/profile.d`, `/etc/profile`, `/etc/bash.bashrc`) → código en cada shell futura, la de root incluida; (c) exports NFS `rw` sin restricción de host; (d) schedules de cron de root con binarios absorbentes de wildcards (`tar`/`rsync`/`zip`/`7z` con `*`); (e) drop-ins individuales de sudoers.d (backlog ronda 8) | **MEDIA** |
| R48 | **CI apagada**: `branches: ain]` (texto corrupto, matchea una rama inexistente) en `on.push` y `on.pull_request` — ni build-and-test ni lab-smoke han corrido sobre main en los últimos commits. La regresión se introdujo aparentemente en el commit `4dfe061` ("ci: fix broken branch filter") que quedó mal aplicado | **CRÍTICA** (reparada por el Rol 4 esta ronda) |
| R49 | Backlog heredado aceptado: GIF re-grabación, tag del mantenedor, docker/ con Docker, CVE-2022-2588 (investigación honesta externa) | ACEPTADOS |

## Decisiones y plan para los roles 2, 3 y 4

- **Bump de versión: v1.6.0 → v1.7.0.** Superficie nueva: dos flags (`--sarif`, `--parallel`), dos categorías de vector (`group`, `hooks`), clave `score` en el summary JSON, bloque SARIF completo y motor concurrente opcional. El tag sigue siendo decisión del mantenedor.
- **Rol 2 (cinco bloques completos, prioridad máxima):**
  1. **Score de endurecimiento** (cierra R44): `hardeningScore` determinista 0–100 con pesos públicos por riesgo y multiplicador 1.5x de explotabilidad, clamped a 0; integrado en `summary.score` del JSON, línea del summary de terminal, fila del markdown, y trayectoria en el diff (`score_after` + `score 60 → 85`; baselines anteriores a la métrica renderizan "baseline predates scoring" en vez de fingir 0→X).
  2. **Export SARIF 2.1.0 `--sarif file`** (cierra R45): log completo (tool/driver/rules/results), reglas deduplicadas por fuente en orden de aparición (nunca iteración de mapa), niveles error/warning/note, ubicaciones `file://` solo para targets-ruta (los demás incrustan el target en el mensaje — nada inventado), escritura 0600.
  3. **Motor `--parallel`** (cierra R46): cada scanner corre en goroutine sobre una COPIA privada de AutoPrivilege (los scanners solo apendizan Findings y leen Opts — la copia es toda la historia de sincronización) y el merge recorre `scannerOrder` por índice: resultados byte-idénticos al secuencial, verificado con test de completación fuera de orden; `--stealth` fuerza secuencial (su jitter existe para dosificar sondas visibles).
  4. **Scanners GROUP + HOOKS + extensiones** (cierra R47): `/etc/group` escribible (HIGH explotable, silencio honesto como root), los cuatro hooks de login (HIGH explotables, guard de forma directorio-vs-fichero), pase por-fichero de sudoers.d condicionado a directorio CERRADO con check de uid root (MEDIUM informativo con paso de verificación `visudo -c` — honestidad sobre el edge-case ACL), heurística cron-wildcard como candidato MEDIUM no-explotable (el hallazgo de fichero escribible sigue ganando por jerarquía), y `nfsExportHostless` para exports rw al mundo (MEDIUM informativo).
  5. **Cableado integral**: `validVectors` +18 nombres, ambos switches de enumeración, vectores manuales honestos para group/hooks, tabla de simetría ampliada, usage + flag descriptions + 2 ejemplos nuevos.
- **Rol 3:** sincronizar ambos README (20 scanners, 24 flags, cuatro formatos de salida, score, SARIF, parallel, 109 tests, CI con -race), paridad flags↔usage flag a flag, búsqueda de ficheros muertos, `go.mod` intacto.
- **Rol 4:** **reparar el filtro de CI (R48)** + paso `-race` en CI, revisión de seguridad de lo nuevo (escritura SARIF, carrera de datos del merge, honestidad de los nuevos riesgos), E2E de namespace con /etc sintético para los scanners nuevos, re-verificación de contratos de exit codes y del stdout del lab.
