# Informe de ronda — AGENTE DE IMPLEMENTACIONES

- **Fecha y hora de la ronda:** 2026-09-15 18h02 (Europe/Madrid)
- **Rol:** 2 de 4 (Agente Único — Implementaciones)
- **Carga de trabajo:** MÁXIMA sostenida. Tres bloques completos: NUEVO vector+fuente POLKIT (scanner, enumeración, exploit reversible, playbook, catálogos), plan de ejecución en JSON para `--dry-run`, flag `--top`. 9 tests nuevos (163/163), `-race` limpio, E2E real.

## Resumen ejecutivo de las mejoras implementadas

1. **POLKIT** — el catálogo crece por primera vez desde la ronda 1: `polkit` es el vector nº 19 y `POLKIT` la fuente nº 18; el README ya podía decir "20 escáneres" con honestidad (19 + este).
2. **`--dry-run --json`** — la previsualización de la escalada ya es consumible por máquinas: clave `plan` estructurada con el veredicto `within_risk`.
3. **`--top n`** — la lista de "Top vectors" del fallo de explotación ya obedece al operador.

## Detalle por bloque

### Bloque 1 — Vector y fuente POLKIT (`polkit.go`, nuevo fichero)

- **Scanner** (`scanPolkit` → `scanPolkitPaths` con paths inyectables, el mismo split que preload/sudoers para tests): vigila `/etc/polkit-1/rules.d` (dir escribible → HIGH explotable; si está cerrado, pase por fichero que caza un `.rules` EXISTENTE escribible — polkitd recarga en cada cambio, el mismo espíritu del pase por fichero de sudoers), y los dirs legacy `localauthority/50-local.d` + `90-mandatory.d` (`.pkla`). Superficies cerradas = SILENCIO total (ausencia honesta de evidencia, pineada por test).
- **Enumeración clasificada por superficie** — honestidad antes que espectáculo:
  - `rules.d` escribible → **vector AUTO** "polkit rule injection": `exploitPolkitRule` planta `00-autoprivilege-temp.rules` (el prefijo `00-` importa: polkitd procesa en orden léxico y el primer callback que decide gana), ejecuta `pkexec /bin/sh -c id`, detecta `uid=0`, y ELIMINA la regla al salir (defer, best-effort) — aditivo y reversible, la misma barra que `exploitSudoersWrite`.
  - fichero `.rules` escribible → **manual** (editar el fichero de política de otro exige confirmación humana; comando exacto con heredoc).
  - dir `localauthority` → **manual** (formato `.pkla` legacy; nota honesta: polkitd moderno lo ignora si hay `.rules` — verificar `polkitd --version`).
- **Payload acotado**: `polkitRulePayload(user)` concede SOLO `org.freedesktop.policykit.exec` SOLO para `subject.user == "<usuario>"` y devuelve `polkit.Result.YES` — pineado por test (prohibido un grant universal).
- **Integración de tablas completa**: `validVectors`, `vectorCatalog` (con descripción honesta), `vectorCatalogOrder` (polkit al final), `validSources`, `explainDB` (playbook de 4 pasos: restaurar permisos, revisar `.rules` línea a línea, auditar localauthority, vigilar logs de polkitd), ambos switches de enumeración (all y por-vector), `scannerOrder`. La tabla estructural de simetría (TestVectorSelectionSymmetry) ampliada con el hallazgo POLKIT — un vector nuevo no puede navegar sin su fuente y viceversa.
- **Usage/paridad**: `--vector` en usage() y registerFlags actualizados con `polkit` — el test hermético obligó la sincronía. La completion hereda `polkit` en los valores de `--vector` sola (sale de `vectorCatalogOrder`).

### Bloque 2 — Plan de ejecución JSON (`--dry-run --json`)

- **`planEntry`** (report.go): `name`/`risk`/`target`/`command`/`kind` (auto|manual)/`within_risk` — el veredicto espeja el `[skip]` del plan de terminal (riesgo > `--risk`).
- **`buildPlan(p)`** función pura sobre `sortedVectors` — la MISMA ordenación safest-first del plan de terminal, así las dos voces no pueden discrepar sobre qué correría.
- **`jsonReport.Plan`** con `omitempty` + `planOrNil(p)`: la clave SOLO existe en dry runs — un run real informa de lo que PASÓ, no de lo que habría pasado. E2E verificado: los 3 vectores manuales de esta máquina aparecen con `within_risk:false` bajo `--risk safe` (por defecto).

### Bloque 3 — `--top n`

- **`Options.TopN`** + `defaultTopVectors = 5` (constante pineada por test) + `topVectorsN()` que resuelve el valor efectivo (construcciones directas sin flag caen al default).
- **Validación fail-fast en `run()`**: 1-50, fuera de rango → exit 2 con el valor y el rango en el mensaje (E2E: `--top 0` y `--top 51` → exit 2). El cero de las construcciones directas nunca pasa por `run()` — documentado en el comentario.
- **Wiring**: `printTopVectors(p, p.Opts.topVectorsN())` sustituye el 5 hardcodeado. Usage es/en actualizado.

## Errores y correcciones de la ronda

- Dos tests POLKIT fallaron por ORDEN de operaciones (chmod 0555 del dir ANTES de crear el fichero dentro → permission denied) — reordenados; y TempDir exigía desbloquear el dir en `t.Cleanup` antes de su RemoveAll.
- Expectativa invertida en TestDryRunPlanJSONShape (los veredictos estaban bien, mi aserción no) — corregida contra la semántica real (LOW dentro de la ventana --risk medium, HIGH fuera).
- Validación de `--top` con guard `!= 0` que dejaba pasar `--top 0` — eliminado el guard (en `run()` el cero solo puede ser un flag explícito), E2E verificado.
- La atomicidad parcial del editor volvió a duplicar un bloque (planEntry insertado dos veces en report.go) — resuelto restaurando el fichero desde git y re-aplicando con script exacto.

## Verificación de la ronda

| Comprobación | Resultado |
|---|---|
| `gofmt -l .` / `go vet ./...` / `go build` | Vacío / OK / OK |
| `go test -count=1 -v` | **163/163 PASS** (154 + 9 nuevos) |
| `go test -race -count=1` | PASS |
| `--version` | v1.8.0 |
| E2E `--list-vectors` (+`--json`) | 19 vectores, `polkit` último en orden canónico |
| E2E `--list-sources` (+`--json`) | 18 fuentes, POLKIT con descripción del playbook |
| E2E `--explain polkit` | playbook completo (4 pasos) |
| E2E `--dry-run --json` | clave `plan` con 3 entradas, `within_risk` correcto bajo `--risk safe` |
| E2E `--top 2` / `--top 0` / `--top 51` | 2 vectores listados / exit 2 / exit 2 |
| E2E `--vector polkit` | selección respeta la simetría estructural (test) |
| Paridad flags ↔ usage | Hermética por test |
| Exit codes 0/1/2/3 | Íntegros |

## Ideas detectadas pero NO implementadas (backlog)

- `pkexec --version` en el hallazgo POLKIT (versión para contexto PwnKit ya cubierto — el scanner de política no la necesita).
- `--top` también para los findings informativos del terminal (hoy solo para vectores).
- `--list-gtfo --json` (heredado).

## Aspectos detectados que corresponden a otros agentes

- **Pulimiento (Rol 3):** screenshot del catálogo regenerado (19), READMEs es/en con POLKIT + plan JSON + `--top`.
- **Bugs/Seguridad (Rol 4):** revisión del exploit POLKIT (acotación, limpieza, reversibilidad) y de los bounds de `--top`.
