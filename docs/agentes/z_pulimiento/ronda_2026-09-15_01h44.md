# Informe de ronda — AGENTE DE PULIMIENTO

- **Fecha y hora de la ronda:** 2026-09-15 01h44 (Europe/Madrid)
- **Rol:** 3 de 4 (Agente Único — Pulimiento)
- **Informes leídos antes de empezar:** los cuatro de la ronda 4 y los del Director e Implementaciones de esta ronda. La deuda de documentación heredada desde la ronda 3 (regenerar el ejemplo de salida con el lab) se cierra aquí con datos REALES.

## Resumen ejecutivo de lo pulido en esta ronda

El frente principal ha sido la veracidad documental: los bloques "Output formats" de ambos README ya no son aproximaciones manuscritas — son la **salida real capturada del lab rootless ejecutado en este entorno** (12 findings, 9 explotables, 9 vectores, `time 1.7s`), con nota honesta de por qué `rooted YES` (mapped root del user namespace). De paso, la captura destapó y corrigió un defecto del script del lab: los mensajes de progreso `[lab] ...` iban a STDOUT y contaminaban la salida `--json` documentada como pipeable — ahora van a stderr y `lab/rootless_lab.sh --json --quiet | jq '.summary'` funciona tal cual se documenta. Además: `DOCKER_HOST` documentado (R32), fila `kernel` con Dirty Cow, badge al recuento final real (60), búsqueda de limpieza ejecutada y verificación final en verde.

## Detalle por cambio

### 1. "Output formats" regenerado con salida REAL del lab (R30 — deuda de la ronda 3)

- El bloque de terminal del README mostraba un formato de summary que ya no existe (sin línea `findings`, sin línea `time`, cabecera `Summary — N exploitable` desaparecida hace rondas) y un JSON de ejemplo no correspondiente a ninguna ejecución real.
- **Nueva captura real:** findings completos del lab (SUID find/python3.13, CRON, FILE×3, CAPS, SERVICE, KERNEL Dirty Pipe) + summary con el formato actual (`findings 12 (exploitable 9)` … `time 1.7s`); JSON con el `summary` real del lab (`rooted: true`). Nota añadida en ambos idiomas: dentro del user namespace la herramienta arranca como root mapeado — de ahí el `rooted YES` honesto.
- Ambos idiomas con estructura idéntica (verificado línea a línea).

### 2. Script del lab: progreso a stderr (fix de pulimiento, no de bug del binario)

- **Causa:** `lab/rootless_lab.sh` enviaba `echo "[lab] building binary..."` y sus hermanos a STDOUT, así que `lab/rootless_lab.sh --json --quiet | jq` fallaba con "Expecting value" — el contrato `--json` documentado del script se incumplía.
- **Solución:** los cuatro mensajes de progreso redirigidos a stderr (`>&2`); verificado: la tubería al JSON ahora es limpia (parseo correcto del objeto completo).
- Sin cambios en el binario: es un defecto del script auxiliar, corregido por Pulimiento porque es pulido de UX del repo, no un bug funcional de la herramienta.

### 3. `DOCKER_HOST` documentado (R32) + fila `kernel` con Dirty Cow

- Nueva nota tras la tabla de vectores (ambos idiomas): los sondeos docker/container heredan el entorno del shell — un daemon configurado vía `DOCKER_HOST` cuenta como alcanzable, y el hallazgo mantiene su "verify rootful vs rootless". Cierra el backlog de Implementaciones de la ronda 3.
- Fila `kernel` de la tabla de vectores: "Dirty Pipe, **Dirty Cow**, OverlayFS, StackRot, nf_tables; PwnKit via pkexec".

### 4. Badge y párrafo de testing

- Badge de tests: 58 → **60 passing** (recuento final real de la ronda: 58 + 2 tests del Rol 2; verificado contra `go test -v`).
- Párrafo de testing: 60 tests (la descripción de cubrimientos ya incluía los rangos de CVEs y la captura/persistencia sgid; sin inflarla con lo que ya describe).

## Archivos innecesarios eliminados en esta ronda

Ninguno. Búsqueda obligatoria ejecutada: sin `.bak`/`.old`/`.tmp`/`.log`/`.orig`/`*~`/`.swp`; `git ls-files` con cero matches de patrones de ficheros muertos. El árbol sigue en 39 ficheros con función + los informes de ronda.

## Estado del README

**Actualizado** (ambos idiomas) con datos reales verificados. Las imágenes (`docs/images/`) siguen vigentes y enlazadas. Pendiente de una futura re-grabación del GIF para que muestre `--list-gtfo` con la sección sgid (sin cambio de formato que lo exija hoy).

## Estado del archivo de dependencias

**Sin cambios necesarios.** `go.mod` (go 1.26, cero dependencias) intacto; el script del lab usa solo bash estándar (bash, mktemp, mount, unshare — verificados en su cabecera). `.gitignore` revisado: sigue cubriendo artefactos de build y salida local de tests; sin entradas obsoletas que retirar.

## Verificación final de la ronda

| Comprobación | Resultado |
|---|---|
| `gofmt -l .` / `go vet ./...` | Limpio / sin avisos |
| `go build` + `--version` | OK — v1.5.0 |
| `go test -count=1 -v` | OK — **60/60** (badge fijado a la cifra real) |
| `lab/rootless_lab.sh --json --quiet \| jq '.summary'` | **OK — JSON limpio por stdout** (fix del script) |
| Ejemplo del README vs ejecución real | Coherente (captura real, no aproximación) |
| READMEs alineados entre sí | Sí |

## Bugs o problemas de seguridad detectados (para el Rol 4)

Heredados y ya resueltos en esta ronda: R29/R30/R31/R32 según el plan del Director. El defecto del script del lab (progreso en stdout rompiendo `--json`) fue detectado, corregido y verificado por Pulimiento — anotado en este informe para trazabilidad. Sin hallazgos nuevos en el binario durante el pulimiento.

## Pendientes de pulido para próximas rondas

- Re-grabar el GIF de demo para mostrar `--list-gtfo` con la sección sgid y el indicador de contenedores endurecido.
- El badge de tests sigue siendo manual (60); candidato permanente: quitarlo o hacerlo dinámico.
- La documentación del lab podría listar explícitamente qué seed usa (passwd/shadow/cron/systemd) — hoy se describe en prosa.
