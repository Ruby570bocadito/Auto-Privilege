# Informe de ronda — AGENTE DE PULIMIENTO

- **Fecha y hora de la ronda:** 2026-09-15 16h24 (Europe/Madrid)
- **Rol:** 3 de 4 (Agente Único — Pulimiento)
- **Informes leídos antes de empezar:** los cuatro de la ronda 14 y los del Director e Implementaciones de esta ronda.
- **Directiva específica del mantenedor esta ronda:** actualizar el README y añadir NUEVAS imágenes y VÍDEOS (medios reales, no mockups).

## Resumen ejecutivo de lo pulido en esta ronda

La galería de medios pasa de 10 a **16 assets** (+2 diagramas SVG, +1 screenshot del modo nuevo, +2 GIFs de sesión real — uno cierra el backlog de la ronda 13 — y +1 VÍDEO MP4 real de 13 s), y los dos READMEs se actualizan a fondo: arquitectura del pipeline, mapa de exit codes, los tres flags nuevos, los modos JSON, las dos GIFs nuevas y el vídeo embebidos/enlazados, recuento 144 → 154. Paridad es/en verificada sección a sección; las 14 referencias de medios de los READMEs resuelven todas; búsqueda de ficheros muertos sin hallazgos; `go.mod` intacto.

## Detalle por cambio

### 1. NUEVOS assets — cadena de producción 100 % real

Cada asset se genera desde la SALIDA REAL del binario de esta ronda (`/tmp/ap15`), capturada con su propio `--color` (los colores son los bytes ANSI que el binario emite, no un terminal falso), renderizada frame a frame con Chromium sobre una plantilla de terminal coherente con las capturas existentes (traffic lights, prompt `z@autopriv-lab`, tipografía monoespaciada) y ensamblada con ffmpeg:

- **`docs/images/architecture.svg`** (NUEVO, diagrama): el pipeline completo de un vistazo — SCAN (20 escáneres) → FILTER (`--ignore`/`--min-risk`) → ENUMERATE (GTFOBins 75+31) → EXPLOIT (opt-in) → REPORT+SCORE (5 formatos, 0600 atómico) → CI GATES (las tres, exit 3) + los cuatro exit codes como banda final. Embebido en "Cómo funciona" de ambos READMEs.
- **`docs/images/exit-codes.svg`** (NUEVO, diagrama): el mapa de decisión de `computeExitCode` — flags inválidos → 2, exploit sin root → 1, puerta tripiada → 3 (gana), resto → 0 — con la nota de que los informes se escriben igualmente. Embebido junto al párrafo de exit codes.
- **`docs/images/screenshot-list-sources.png`** (NUEVO): el modo `--list-sources` de esta ronda — las 17 fuentes con sus descripciones + la cabecera `--list-sources --json` real. Embebido junto al screenshot del catálogo de vectores.
- **`docs/images/demo-min-score.gif`** (NUEVO — **cierra el backlog de la ronda 13**): la tercera puerta por fin con demo animada — contexto (score 40/100), `--min-score 70` tripia con `[!] score gate: hardening score 40 < 70 — exit 3` y `exit=3`, y `--min-score 30` pasa con `exit=0`. El exit del binario se muestra con el idioma honesto de bash (`${PIPESTATUS[0]}`).
- **`docs/images/demo-min-risk.gif`** (NUEVO): el suelo de riesgo en acción — sin filtro (17 hallazgos, score 40), con `--min-risk medium` (4 hallazgos, score 66) y `--json | jq -c '.summary'` demostrando que las máquinas ven el mismo conjunto filtrado.
- **`docs/videos/demo-full-session.mp4`** (NUEVO — primer VÍDEO del repo): 13 s a 24 fps (libx264, yuv420p, faststart, 302 KB): el scan íntegro con banner coloreado, el suelo `--min-risk medium` (score 40 → 66), la puerta 70 tripiando (exit 3) y la puerta 30 en verde (exit 0). Enlazado desde la cabecera de ambos READMEs ("▶ Mira la sesión completa como vídeo MP4 real").

### 2. README.md y README.es.md actualizados a fondo

- **Cabecera**: nota de medios con enlace al MP4 real (es/en con texto localizado).
- **Lo más importante**: fila nueva "Catálogos máquina-legibles" (`--list-vectors --json`, `--list-sources`, `--explain --json`, sobre común); fila "Amigable para scripts" ahora menciona `--color`.
- **Cómo funciona**: diagrama `architecture.svg` embebido tras los tres pasos.
- **Usage**: bloques es/en sincronizados con el binario — `--list-sources` en Modos, `--color` en Salida, `--min-risk` (3 líneas) en Varios.
- **Exit codes**: mapa `exit-codes.svg` + párrafo extendido de `--list-vectors --json` / `--list-sources` + screenshot del modo nuevo.
- **Formatos de salida**: sección nueva "**Suelo de riesgo**" con el contrato completo (aplicado una vez, mismo contrato que `--ignore`, suelo inclusivo, `safe` rechazado con pista) + GIF; la puerta de postura gana su GIF (backlog cerrado); completion ahora documenta `--ignore <TAB>` y `--min-risk <TAB>`; playbook documenta la voz `--explain --json`.
- **Tests y CI**: 144 → **154** con el desglose de la ronda 15 (catálogos pineados, suelo inclusivo + no-mutación + puertas-ven-lo-filtrado, precedencia a tres bandas de `--color`, listas de completion nuevas).
- Paridad es/en verificada sección a sección.

### 3. Verificación de medios

- Las **14 referencias** de medios de los dos READMEs resuelven todas a ficheros existentes (11 imágenes + 2 SVG + 1 vídeo).
- Ambos SVG validan como XML bien formado.
- Los GIFs verificados visualmente frame a frame: colores reales del binario (rampa del banner, tags de riesgo, bold de fuentes), tecleo sobre la misma línea, exits correctos (3 → 0 en la puerta postural).

### 4. Archivos innecesarios eliminados en esta ronda

Ninguno. Búsqueda obligatoria sobre `git ls-files` (`.bak`/`.old`/`.tmp`/`.log`/`.orig`/`*~`/`.swp`): cero matches. Los frames de captura viven en `/tmp`, fuera del repo; los scripts generadores viven fuera del repo (el repo lleva solo los assets finales).

### 5. Estado del archivo de dependencias

**Sin cambios necesarios.** `go.mod` intacto (go 1.26, cero dependencias): los catálogos JSON usan `encoding/json`/`sort`/`strings`/`fmt` (stdlib); `filterMinRisk`/`parseMinRisk` no añaden nada externo. La cadena de medios (playwright + ffmpeg) es tooling de captura, no dependencia del producto.

### 6. Verificación final de la ronda

| Comprobación | Resultado |
|---|---|
| `gofmt -l .` | Vacío |
| `go vet ./...` / `go build` | OK / OK |
| `go test -count=1 -v` | **154/154 PASS** |
| `go test -race -count=1` | PASS |
| READMEs alineados entre sí | Sí (es/en sección a sección) |
| Referencias a medios en READMEs | Las 14 resuelven |
| SVGs | XML válido, embebidos en la sección correcta |
| GIFs (contenido) | Salida real, colores reales, exits verificados |
| MP4 | 13.4 s, moov presente (faststart), 302 KB |
| Búsqueda de ficheros muertos | Ejecutada — ninguno |
| `go.mod` | Intacto, stdlib only |
| `--version` | v1.8.0 |

### 7. Bugs o problemas detectados (para el Rol 4)

- El entorno de captura resultó hostil por partida doble: `NO_COLOR=1` de fábrica (el binario lo respeta correctamente — correcto) y un pty que FILTRA las secuencias CSI (los escapes llegaban mutilados al master). La solución correcta no fue rodear el binario sino darle a la herramienta su propio `--color` — convertida en la quinta mejora del Rol 2 y verificada con bytes ANSI reales por pipe.
- Editor con atomicidad parcial en MultiEdits sobre los READMEs: re-verificado el estado con `git diff` tras cada lote; las ediciones quedaron completas y sin duplicados (auditado con grep sección a sección).

## Pendientes de pulido para próximas rondas

- **Taguear v1.8.0** (acumulado; el código ya lo lleva pineado).
- `docker/` con Docker disponible (heredado).
- Los GIFs antiguos (demo-scan, demo-ci-gate) podrían regenerarse con la misma cadena `--color` para uniformidad de estilo — bajo prioridad, los actuales son correctos.
