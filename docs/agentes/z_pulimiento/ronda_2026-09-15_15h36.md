# Informe de ronda — AGENTE DE PULIMIENTO

- **Fecha y hora de la ronda:** 2026-09-15 15h36 (Europe/Madrid)
- **Rol:** 3 de 4 (Agente Único — Pulimiento)
- **Informes leídos antes de empezar:** los cuatro de la ronda 11 y los del Director e Implementaciones de esta ronda.
- **Prioridad del mantenedor ejecutada:** actualizar el README y añadir NUEVAS imágenes y videos.

## Resumen ejecutivo de lo pulido en esta ronda

La deuda de medios más antigua del repo (GIF de demo re-grabable, pendiente desde tres rondas atrás) queda saldada con creces: **3 GIFs animados nuevos + 4 screenshots de terminal + 1 screenshot de navegador**, TODOS generados de ejecuciones REALES del binario (cero mockups, cero datos inventados). Los ambos README incorporan la galería, los tres flags nuevos y el recuento real (133 tests); paridad flags↔usage ahora hermética por test; búsqueda de ficheros muertos sin hallazgos; `go.mod` intacto.

## Detalle por cambio

### 1. Nueva galería de medios (docs/images/) — salida real, renderizado fiel

**Cadena de producción:** el binario real se ejecutó en un pty (capturador Python `pty.fork`, sin `script` — su line-discipline corrumpía las secuencias CSI) con `NO_COLOR` desactivado del entorno y TERM=xterm-256color; la salida ANSI cruda se parsea (SGR 3/4-bit, 256-color y truecolor, bold) y se renderiza a frames PIL con DejaVu Sans Mono sobre fondo #0d1117 con barra de título; los GIFs animan el tecleo del comando real y el revelado línea a línea con cursor, en loop infinito.

| Asset | Contenido (TODO real) | Uso |
|---|---|---|
| `demo-scan.gif` (300KB, 36f) | Scan completo: banner con rampa cian→verde, 3 hallazgos HIGH (KERNEL Dirty Pipe + 2 CRED), summary score 40/100 | Hero del README (arriba) |
| `demo-ci-gate.gif` (188KB, 79f) | Sesión CI completa: snapshot `--output base.json` → `echo 'GITHUB_TOKEN=…' >> ~/.zsh_history` → `--baseline … --fail-on-new=high` → `regression gate … at/above HIGH — exit 3` → `echo $?` → **3** | Hero del README (arriba) |
| `demo-explain.gif` (58KB, 18f) | `--explain cron`: playbook CRON completo | docs |
| `screenshot-scan.png` (65KB) | Frame final del scan | docs |
| `screenshot-ci-gate.png` (27KB) | Frame final de la sesión del gate | docs |
| `screenshot-explain.png` (23KB) | Frame final del playbook | Formatos de salida (ambos README) |
| `screenshot-vectors.png` (90KB) | `--list-vectors`: catálogo de 18 vectores | Tras la línea de exit codes (ambos README) |
| `screenshot-html-report.png` (274KB) | El `audit.html` REAL (generado por el binario de esta ronda contra esta máquina) renderizado en Chromium (device_scale 2): tarjetas, barra de riesgos, tabla con badges | Formatos de salida, párrafo HTML (ambos README) |
| `demo-lab.gif` (3600KB, heredado) | Re-embedido en su sección natural: "The rootless lab" / "El laboratorio rootless" (había quedado huérfano al rotar el hero) | Sección del lab |

- **Honestidad de la sesión del gate:** el trigger fue una regresión REAL — la primera intentona (secreto en `~/.bash_history`, ya en baseline) NO tripó la puerta porque la clave fuente+objetivo coincidía: exactamente la robustez de `findingKey` de la ronda 11 observada en vivo. Se usó `~/.zsh_history` (target nuevo) y el exit 3 + `echo $?` capturados son los de esa ejecución real.
- El GIF heredado `demo-lab.gif` se conserva (3.6MB, sigue siendo la mejor demo de escalada real a uid=0).

### 2. README.md y README.es.md sincronizados con el estado real

- **Hero:** el GIF de lab sale del hero; entran `demo-scan.gif` + `demo-ci-gate.gif` con nota "Real terminal output — no mockups".
- **Highlights:** "Four output formats" → **"Five output formats"** (con `--html`); fila nueva **"Two CI gates"** (política vs regresión, con umbral).
- **Usage:** `--html file`, `--list-vectors`, `--fail-on-new [risk]` con su umbral; ejemplos nuevos (`--html audit.html`, `--fail-on-new=high`); línea de exit codes actualizada (ambas puertas en el `3`).
- **`--list-vectors` documentado** con su screenshot y la promesa de anclaje ("leída de la misma tabla que usa el binario, la documentación nunca puede divergir del código").
- **Formatos de salida:** párrafo **HTML** completo (autocontención, file:// air-gapped, mismo buildReport del JSON, escape XSS testeado, 0600 atómico) + screenshot del navegador; párrafo **Hardening playbook** nuevo con su screenshot; párrafo CI gates reescrito con el umbral (`=high` solo con regresiones HIGH+, invalid threshold exit 2).
- **Testing y CI:** recuento 124 → **133** con el desglose de la ronda 12 (HTML: XSS/estructura/0600/diff/vacío-seguro; umbral; parseo del flag; catálogo anclado; paridad hermética) y la lista de escritores atómicos ampliada a `--html`.
- Paridad es/en verificada sección a sección (mismas imágenes, mismos bloques, mismas cifras).

### 3. Paridad usage ↔ flags

Ahora **garantizada por `TestFlagUsageParity`** (FlagSet privado vía `registerFlags`, comparación bidireccional con `--help` whitelist): 31 flags registrados ↔ 31 en usage. La comprobación manual de las rondas anteriores ya no puede dormirse.

### 4. Archivos innecesarios eliminados en esta ronda

Ninguno. Búsqueda obligatoria sobre `git ls-files` (`.bak`/`.old`/`.tmp`/`.log`/`.orig`/`*~`/`.swp`): cero matches. Los temporales de captura y render (pty raw, frames) vivieron solo en `/tmp`, fuera del repo.

### 5. Estado del archivo de dependencias

**Sin cambios necesarios.** `go.mod` intacto (go 1.26, cero dependencias): `html.go` usa `html`/`fmt`/`sort`/`strings`/`time` (stdlib); el render de medios es tooling externo (scripts fuera del repo, Python/PIL), no toca el módulo Go.

### 6. Verificación final de la ronda

| Comprobación | Resultado |
|---|---|
| `gofmt -l .` | Vacío |
| `go vet ./...` / `go build` | OK / OK |
| `go test -count=1 -v` | **133/133 PASS** |
| `go test -race -count=1` | PASS |
| READMEs alineados entre sí | Sí (es/en comparados sección a sección) |
| Paridad flags ↔ usage | Hermética por test |
| Referencias a imágenes en READMEs | Las 9 resuelven (8 en docs/images + banner) |
| GIFs: loop infinito, tamaños | Verificado (PIL: n_frames, loop=0) |
| Búsqueda de ficheros muertos | Ejecutada — ninguno |
| `go.mod` | Intacto, stdlib only |
| `--version` | v1.7.0 |

### 7. Bugs o problemas de seguridad detectados (para el Rol 4)

- **Barra de riesgos del HTML mapeaba el bucket equivocado** ("INFO" vs el "SAFE" real de buildSummary) — cazado en la revisión del Rol 4 y corregido antes del commit (los informativos se pintan ahora). Pendiente de su verificación.
- Primera cadena de captura (util-linux `script`) CORRUPIA las secuencias CSI — sustituida por capturador pty propio; los assets finales están regenerados íntegramente con la cadena sana.

## Pendientes de pulido para próximas rondas

- **Tag v1.8.0** (recomendación del Director acumulada desde la ronda 11 — decisión del mantenedor).
- Screenshot del informe HTML con sección de diff visible (la captura actual muestra summary + findings).
- Comprobación de `docker/` con Docker disponible (heredado).
- Variante oscura del CSS del HTML (backlog del Rol 2).
